import { ResourcePathPipe } from './resource-path.pipe';

describe('ResourcePathPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourcePathPipe();
    expect(pipe).toBeTruthy();
  });
});
